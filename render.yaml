services:
  - type: web
    name: mist-agencies-invoice
    env: docker
    plan: free
    dockerfilePath: Dockerfile
    healthCheckPath: /api/template
    envVars:
      - key: PORT
        value: 5000
      - key: NODE_ENV
        value: production
